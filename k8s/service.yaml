apiVersion: v1
kind: Service
metadata:
  name: banking-api-service
  namespace: banking
  labels:
    app: banking-api
spec:
  selector:
    app: banking-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
