# Lesson 01 — Endpoint + Service (Intermediate)

**Level:** Intermediate

## Goal

Typed HttpClient calling an external rate API, IHttpClientFactory, async/await, CancellationToken, IOptions<T> for base URL.

## Instructions

1. Read through the code changes in this branch (use `git diff` against the previous part).
2. Run the project and test the endpoints via the OpenAPI UI at `/openapi`).
3. Experiment — break things, fix them, extend them.
4. When ready, move to the next part branch.

